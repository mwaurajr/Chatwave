<div class="absolute top-0 left-0 m-4">
  <% if user_signed_in? %>
    <p class="text-sm">
      <%= "Good #{time_of_day}" %> <%= name(current_user) %>! 👋
    </p>
  <% else %>
    <div>
      <h2 class="mt-8 text-2xl font-bold leading-9 tracking-tight text-gray-900">Kindly Sign in to continue</h2>
      <p class="mt-2 text-sm leading-6 text-gray-500">
      <%= link_to "Login in here",
        new_user_session_path,
        class: "font-semibold text-indigo-600 hover:text-indigo-500" %>
      </p>
    </div>
  <% end %>
</div>
